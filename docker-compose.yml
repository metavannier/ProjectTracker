version: "3.9"

services:
  web:
    build: .
    container_name: projecttracker_web
    ports:
      - "4020:80"
    networks:
      - projectnet
    volumes:
      - ./projects.json:/app/projects.json           # Main projects file
      - ./project:/app/project                       # Individual project files

networks:
  projectnet: